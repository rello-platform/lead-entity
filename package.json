{
  "name": "@rello-platform/lead-entity",
  "version": "0.1.0",
  "description": "Pure-function classifier + normalizer for Rello Lead entity-shape (LLC, partnership, trust, corporation, individual). Consumed by Harvest-Home (BYOL/intake/merge writers), Rello (createLead validation + write-time fill), and the Phase 7 backfill script. Single source of truth for entity-name normalization across the platform.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "src", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  },
  "repository": {
    "type": "git",
    "url": "github:rello-platform/lead-entity"
  },
  "license": "UNLICENSED",
  "private": true
}
